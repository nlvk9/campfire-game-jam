export function drawTiles(globals)
{
    const canvas = document.getElementById(globals.canvas)
    const level = globals.level



}

function getImageFromTileset(globals, index)
{
    const x = index % 64
    const y = Math.trunc(index / 64)
    




}